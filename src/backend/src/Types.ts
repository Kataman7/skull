export enum CardType {
  Flower = 'flower',
  Skull = 'skull',
}

export enum ActionType {
  Bet = 'bet',
  Pick = 'pick',
  LooseCard = 'looseCard',
  PlayCard = 'playCard',
  GameOver = 'gameOver',
  Join = 'join',
  Leave = 'leave',
  Skip = 'skip',
}